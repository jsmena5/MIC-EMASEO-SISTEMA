--extra-index-url https://download.pytorch.org/whl/cpu
fastapi==0.115.0
uvicorn[standard]==0.30.6
gunicorn==22.0.0
celery[redis]>=5.3.6
pillow>=10.0.0
ultralytics==8.3.0
torch==2.1.2+cpu
torchvision==0.16.2+cpu
timm>=0.9.0         # requerido por MiDaS_small (backbone EfficientNet via torch.hub)
open-clip-torch>=2.24.0  # gate semántico: distingue basura real de personas/interiores/pantallas
# ── gRPC ───────────────────────────────────────────────────────────────────────
grpcio>=1.64.0
grpcio-tools>=1.64.0
# ── S3 (descarga de imagen en el worker, sin pasar Base64 por la red interna) ──
boto3>=1.34.0
