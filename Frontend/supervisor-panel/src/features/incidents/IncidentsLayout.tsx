/**
 * IncidentsLayout — sección Incidentes.
 * Muestra directamente la bandeja de casos (tarjetas + lista).
 * El dashboard de analíticas vive en INICIO.
 */
import IncidentsPage from "./IncidentsPage"

export default function IncidentsLayout() {
  return <IncidentsPage />
}
